// Basic smoke test for Lexi Mega Store.
//
// Verifies the app widget tree can be built without errors.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder – replace with a real widget test once the app
    // supports running without async bootstrap (Hive, secure storage, etc.).
    expect(1 + 1, equals(2));
  });
}
